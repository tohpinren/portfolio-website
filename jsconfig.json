{
  // JavaScript Project Configuration
  //
  // This file configures the JavaScript language service and provides
  // IntelliSense support for the project in editors like VS Code.
  // It also sets up import path mapping for cleaner imports.
  //
  // Key Features:
  // - Enables absolute imports from project root using @ prefix
  // - Improves developer experience with better autocomplete
  // - Reduces relative import complexity (../../components becomes @/components)
  //
  // @see https://code.visualstudio.com/docs/languages/jsconfig

  "compilerOptions": {
    // Path mapping configuration for import resolution
    // This allows using absolute imports with @ as an alias for the project root
    "paths": {
      // Map @/* to project root (./*)
      // Example: import PageLayout from '@/components/PageLayout'
      // Instead of: import PageLayout from '../components/PageLayout'
      "@/*": ["./*"]
    }
  }
}
